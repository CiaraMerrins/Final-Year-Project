


<div align="center"><i><h3> <%= @post.player.name %>'s Diary Entries</h3></div></i>
<p class= "body">

<font size="3"><li><%= @post.body %></li></font>
</p>
<p class="date">
Submitted <%= time_ago_in_words(@post.created_at) %> Ago
<br>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: 'Are you sure?'} %>
<br>
<br>
<div id="comments">
<i><h3><div align="center">Number of comments from your Coach: <%= @post.comments.count %></h3></div></i>

	
	

<% @post.comments.each do |comment| %>
	
		<tr><td>
		<li><strong>Section:</strong>
		<%= @post.title.name %></li>
		
		
		<table border="1" style="width:25%">
	<tr>
	<td><p><%= comment.body %></td></p>
	</tr>
	


</div>
</div>
</table>
<p class="date">
Submitted <%= time_ago_in_words(comment.created_at) %> Ago
	
	<br>
	
	<td><%= @post.player.coach.name %></br>
	<br>
	<% end %>