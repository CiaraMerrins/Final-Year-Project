<%= form_for @post do |f| %>
<% if @post.errors.any? %>
<div id="errors">
<h2><%= pluralize(@post.errors.count, "error") %> prevented this post from saving</h2>
<ul>
<% @post.errors.full_messages.each do |msg| %>
<li><%= msg %> </li>
<% end %>
</ul>
</div>
<% end %>

  <div class="field">
    <%= f.label :title_id %> :
    <%= f.collection_select :title_id, Title.all, :id, :name %> 
  </div>

<p>
<%= f.label :description %><br>
<%= f.text_area :body, :size=>"33", placeholder: "Leave a Diary Entry ", :maxlength=>"230" %>
</p>

<p>
<%= f.submit "Submit Diary Entry",class: "btn btn-success" %>
</p>
<% end %>